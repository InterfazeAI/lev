[project]
name = "lev-workspace"
version = "0.1.1"
description = "lev — umbrella workspace: the `lev` decision model and the `levbench` harness"
readme = "README.md"
requires-python = ">=3.12"
license = { text = "Apache-2.0" }
dependencies = [
    "levbench",
    "lev",
]

[project.optional-dependencies]
train = ["lev[train]"]
serve = ["lev[serve]"]
modal = ["lev[modal]"]
demo = ["levbench[demo]"]

[dependency-groups]
dev = [
    "pytest>=8.3",
    "ruff>=0.14",
    # Resolve the SDK rather than the local modal/ directory during type checking.
    "modal>=1.0",
]

[tool.uv.sources]
levbench = { workspace = true }
lev = { workspace = true }

[tool.uv.workspace]
members = ["packages/*"]

[tool.pytest.ini_options]
testpaths = ["packages/levbench/tests", "packages/lev/tests"]
addopts = "-q"

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]
