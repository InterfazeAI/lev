"""Benchmark harness for any `/v1/systemone` server: Jev, lev, or an LLM baseline.

Import submodules directly (`from levbench import runner, metrics`); the package
binds nothing at import time, so importing one module does not pull in the rest.
"""
